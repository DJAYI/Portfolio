---
interface Props {
  title: string;
  link: string | URL;
}

const title = Astro.props.title;
const link = Astro.props.link;
---

<a
  href={link}
  class="py-2 px-4 font-semibold w-fit mx-auto hover:bg-slate-800 mt-4 bg-black transition rounded-full"
  >{title}</a
>
